# PropFirmTW (PFTW) SEO 與關鍵字策略｜2026-07-28

資料來源：Google 台灣（gl=tw, hl=zh-TW）搜尋建議 API 的實際回傳與相關度分數，以及 Google 搜尋結果的競爭者盤點。搜尋建議的排序反映真實搜尋熱度，可當作「相對搜尋量」的免費替代指標；精確每月搜尋量仍需 Keyword Planner 或 Ahrefs。

---

## 一、這次已經動的（程式碼已 commit：`4fba138`）

首頁 `index.html` 把品牌名補成 `PropFirmTW (PFTW)`，加上 canonical、keywords，並新增三段結構化資料：Organization（把 PFTW、PropFirm TW、台灣 Prop Firm 都列為 alternateName）、WebSite、以及一組 FAQPage，其中第一題就是「PFTW 是什麼？」——這是讓 Google 把 PFTW 這個縮寫和你的網域綁在一起最直接的作法。

五個規則頁（lucid、apex、topstep、tradeday、tradeify）先前**完全沒有 meta description、沒有 canonical、沒有 OG、沒有結構化資料**，只有一個 title。這是整站最大的漏洞，因為這幾頁才是能吃到長尾流量的頁面。現在每頁都補上了針對性的 description、keywords、canonical、OG 卡片，以及 Article + BreadcrumbList 兩段 JSON-LD。標題也重寫成前置關鍵字的版本，例如 Topstep 那頁從「Topstep 完整規則 2026」改成「Topstep 規則 2026｜出金、日虧損上限、Combine 考核完整教學」。sitemap 補上 lastmod。

---

## 二、關鍵字池（依 Google 台灣實際建議熱度排序）

### 第一梯：Lucid —— 你在台灣最有機會贏的字

在台灣搜尋 `lucid`，Google 的第一順位建議就是 **`lucid trading`**（相關度 1250，壓過 lucid dream、lucid air 這些字），代表台灣搜這個字的人有相當比例在找 prop firm，不是找電動車或清醒夢。這跟你的直覺一致，Lucid 值得當主攻。

延伸長尾（依建議順序）：`lucid trading 介紹`、`lucid trading 教學`、`lucid trading 登入`、`lucid trading 折扣`、`lucid trading 一致性`、`lucid trading ptt`、`lucid trading pro`、`lucid trading 優惠碼`、`lucid trading 詐騙`、`lucid trading 盜刷`、`lucid trading direct`。

值得注意「一致性」「詐騙」「盜刷」這三個——搜這些的人焦慮、決策期、轉換率高，而且幾乎沒有中文內容認真寫。建議各開一個獨立段落或子頁。

### 第二梯：Topstep —— 長尾最完整、你 GSC 已有點擊

`topstep 考試`、`topstep live 規則`、`topstep 折扣碼`、`topstep 優惠碼 2026`、`topstep 50k`、`topstep payout`、`topstep 詐騙`、`topstep 取消訂閱`、`topstepx`、`topstep 儀表板`。

`topstep 取消訂閱` 和 `topstepx` 是幾乎沒人做的空白區，難度極低。`topstep 優惠碼 2026` 直接帶轉換。

### 第三梯：Apex —— 一定要加 trader funding，不能只寫 apex

你的判斷正確。單獨的 `apex` 在台灣被《Apex 英雄》完全佔據，搶了也只會拿到玩家流量，跳出率高又不轉換。但加上 trader 之後就乾淨了：`apex trader funding`（相關度 1250）、`apex trader 規則`、`apex trader funding 教學`、`apex trader funding 規則`、`apex trader funding 考試`、`apex trader 啟動費`、`apex trader funding tradovate`、`apex trader funding payout rules`。

所以規則頁的 title 已改為以 `Apex Trader Funding` 開頭，全站也不要出現單獨的「Apex」當作 SEO 標的。

### 第四梯：TradeDay / Tradeify

TradeDay：`tradeday 規則`（相關度 1000，是這組裡最高的）、`tradeday 2.0`、`tradeday payout`、`tradeday quickpay`、`tradeday scalping rules`、`tradeday restricted countries`、`tradeday evaluation rules`。

Tradeify：`tradeify 是什麼`、`tradeify select`、`tradeify growth`、`tradeify lightning funded`、`tradeify 出金方式`、`tradeify growth 出金`、`tradeify kyc`、`tradeify 優惠`。

### 第五梯：類別字（量大但競爭高，中期再打）

`prop firm 是什麼`（這組裡的最高相關度 1250）、`prop firm match`、`prop firm 平台`、`prop firm ptt`、`prop firm 報稅`、`prop firm 中文`、`prop firm dcard`、`prop firm 台灣`、`prop firm 交易`。

**`prop firm 報稅` 是這裡面最被低估的一個。** 搜這個的人已經在賺錢、已經出金、對平台有黏著度，而且中文幾乎零內容。建議獨立寫一頁「台灣人 Prop Firm 出金要不要報稅」，這頁的價值可能超過任何一篇規則整理。

---

## 三、一個要修正的方向：別再主打「期貨自營商」

網站目前多處用「期貨自營商」當關鍵字，但在台灣搜這個詞的人，Google 建議跑出來的是「期貨自營商營業保證金」「期貨自營部」「期貨自營商 法規」「專營期貨商」——這些是找元大、凱基那種受金管會監管的自營部門，跟 prop firm 是完全不同的東西。搜這個字的人進來會馬上跳出，對排名反而有害。

建議改用台灣交易圈實際會講的說法：`Prop Firm`、`自營交易公司`、`資助帳戶`、`funded account`，並保留 `prop firm 是什麼` 這種教育型入口。

---

## 四、競爭對手（目前佔著你想要的位置）

`homedadpro.com`（全職奶爸）目前同時吃下「Prop Firm 是什麼」「Prop Firm 考試攻略」「Topstep 規則完整教學」三個核心位置，是最主要的對手。其餘還有 `propfirmsdrama.com`（更新快，走新聞型）、`marketmates.com`、`brownie1688.com`、`hutchinst.com`。

他們的共同弱點是：規則寫得淺、更新頻率不穩、沒有做結構化資料、沒有做各家「一致性規則 / 詐騙疑慮 / 取消訂閱」這類焦慮型長尾。你的規則頁深度其實已經勝出，缺的只是技術 SEO 訊號（剛補上）和索引速度。

---

## 五、還沒完成、需要你的帳號才能做的三件事

第一件是 **push**。程式碼已經 commit 在本機（`4fba138`），但我這邊沒有 GitHub 認證，推不上去。在 liquid-glass 資料夾開終端機執行 `git push origin main` 即可。

第二件是 **www 與非 www 重複索引**。GSC 顯示 `propfirmtw.com` 和 `www.propfirmtw.com` 同時有點擊，等於排名權重被切兩半。canonical 已經統一指向非 www，但根治要在主機或 DNS 端設 301，把 www 全部導到非 www。

第三件是 **到 GSC 逐頁催索引**。網址審查貼上 `propfirmtw.com/rules/lucid.html`、`/rules/topstep.html`、`/rules/tradeday.html`、`/rules/tradeify.html`、`/rules/apex.html`、`/compare.html`，各按一次「要求建立索引」。新站這是最快的方式。

---

## 六、接下來三個月的內容順序（建議）

先把 Lucid 那頁拆深，把「一致性規則」「是不是詐騙」「盜刷疑慮」各寫成獨立段落並加 FAQ 結構化資料。接著開「台灣人 Prop Firm 出金報稅」單頁。再來補 Topstep 的「取消訂閱」「TopstepX 平台」兩個空白長尾。最後才回頭做 `prop firm 是什麼` 這種大詞的教育型長文，那時候站的權重才撐得住。
